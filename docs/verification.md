# v1.1.0 验证记录

验证日期：2026-09-03。

## 自动化

- `npm test`：17 项测试全部通过。
- `npm run build`：前端生产构建通过。
- `npm audit --omit=dev`：本次检查未报告生产依赖漏洞。

覆盖：匿名与待授权访问、员工隔离、主管团队范围、产品经理只读、管理员配置权限、员工提审锁定与退回重提、主管修订发布、历史版本保护、下架和恢复、默认版本、并发与重复请求、CSRF、主管调整与资产交接、上传大小和数量限制、失败清理、删除限制、备份恢复。

飞书认证采用模拟服务响应测试，验证浏览器绑定 state、PKCE 请求、一次性回调、企业 tenant_key 限制、首位管理员初始化和新用户待授权。没有使用真实企业飞书凭据联调。

文件下载测试在带点号的隐藏数据目录下运行，覆盖实际 `.preview-data/` 场景。

## Docker 部署与验收（本次）

- `docker compose up -d --build --wait` 构建、启动成功；容器 `prt-datahub` 健康检查为 `healthy`。
- 本机入口 `http://127.0.0.1:8080`；端口实际只发布至 `127.0.0.1`。
- `node scripts/smoke-docker.mjs` 通过：合成员工上传 ZIP → 提交 → 主管审核发布 → 产品经理下载 → 校验 SHA-256；其他组员工及纯管理员无法读取该资产。
- 管理员备份接口返回成功，文件复制和 SHA-256 校验完成。
- `docker compose restart` 后执行 `node scripts/smoke-docker.mjs --verify` 通过，同一资产的文件、发布版本、提交快照、操作记录和备份时间均保持。
- 验收资产名：`容器验收 · 零售数据样例`。具体标识及校验值记录于被 Git 忽略的 `test-results/docker-smoke.json`。
- 新增测试覆盖分页/搜索转义、归属人选项权限隔离、已有发布版本筛选、手动备份及容器体验模式约束。
- Docker 登录页已加载检查；浏览器自动审批拒绝点击演示登录，理由为登录操作需明确授权，因此本次未完成登录后的浏览器页面复查。接口验收在该拦截前已独立完成。

## 页面与运行服务（前次本地开发验证）

- 本地登录页和主管／产品经理工作台可正常显示。
- 已在页面修改演示员工数据说明、保存并审核发布，状态更新为已发布，归属人保持不变。
- 已通过页面上传真实 ZIP 测试包，文件、说明和归属人保存成功，显示文件大小与校验值。
- 首次页面下载检查发现隐藏数据目录导致 404，已修复并加入测试运行目录约束。
- 修复后通过正在运行的前端代理发起授权下载，返回 HTTP 200、601 字节，ZIP 签名及附件响应头正确。
- 修复后内置浏览器的文件保存事件未再次验证；下载接口及实时服务返回已验证。
- 已检查主要页面浏览器日志，检查时未发现应用报错。

## 尚需部署环境验证

- 企业飞书应用真实登录、应用可用范围、tenant_key 和 HTTPS 回调。
- 内网服务器、防火墙或 VPN 访问范围及反向代理上传设置。
- 企业实际大文件与网络条件下的并发和容量。
- 本机 Docker 已执行部署及重启验收；最终企业服务器的定时备份和恢复演练尚待运维落实。


## 真实飞书登录联调补充

2026-09-03：新应用凭据校验成功，企业信息权限生效并自动取得企业标识；本机真实登录容器健康检查通过。用户完成飞书授权，平台成功识别刘昊霖并进入待授权页；完成首位管理员初始化后，浏览器已验证进入人员与权限页，成员显示管理员、已启用。企业正式服务器 HTTPS 部署仍未执行。
