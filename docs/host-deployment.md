# Windows 宿主机部署

2026-09-03：正式飞书环境已从 Docker 转为 Windows 原生 Node.js 运行，监听 `0.0.0.0:8080`。访问地址：http://192.168.110.54:8080 。

## 当前运行配置

- Node.js 24，前端由 `npm run build` 生成，Express 同时提供页面和 API。
- `.env.host` 保存飞书凭据、企业标识和运行参数；不要提交或分享此文件。
- `APP_ORIGIN=http://192.168.110.54:8080`，飞书重定向地址为 `http://192.168.110.54:8080/api/auth/callback`。
- 真实数据位于 `host-data/`；迁移校验备份位于 `backups/host-migration-2026-09-03T08-41-36-477Z/`。
- 保留原管理员刘昊霖及原有数据；迁移备份不保留会话，需重新登录。
- 原 `prt-datahub-login` 容器已停止，数据卷保留。不要与宿主机服务同时占用 8080；切回 Docker 前应先备份并同步宿主机新增数据。
- 防火墙规则 `PRT-DataHub-LAN-8080` 仅允许本子网访问本机 `192.168.110.54:8080`。旧端口代理已移除。

## 启停和维护

在项目目录执行：

```powershell
.\scripts\start-host.ps1
.\scripts\stop-host.ps1
```

后台守护进程会在服务意外退出后等待 5 秒重新启动。日志：`.host-runtime/server.log`（超过 10 MB 保留上一份）。

已安装当前 Windows 用户的登录启动快捷方式 `PRT DataHub.lnk`，登录 Windows 后隐藏启动。它不是系统服务；无人登录时不保证启动，电脑休眠或关机会中断访问。可运行 `scripts/install-host-startup.ps1` 重建快捷方式；取消自动启动可删除 Windows 启动文件夹内此快捷方式。

更新代码后执行：

```powershell
npm run build
.\scripts\stop-host.ps1
.\scripts\start-host.ps1
```

在线备份：

```powershell
node --env-file=.env.host scripts/backup.mjs
```

IP 改变时，需要一起更新 `.env.host` 的 `APP_ORIGIN`、飞书重定向地址及防火墙本机地址，然后重启服务。请从统一的局域网地址发起飞书登录，避免本机地址与局域网地址混用导致 OAuth 状态 Cookie 不匹配。

## 验证记录

- 宿主机回环地址与局域网地址的 `/api/health` 均返回 HTTP 200；浏览器登录页面正常显示。
- 17 项自动化测试全部通过，包括 OAuth 状态校验、租户隔离、权限、版本流程及备份恢复。
- 浏览器已从局域网入口进入飞书授权确认页；自动审批拒绝代用户点击授权，迁移后的真实授权回调仍待用户点击确认后验证。

## 手机访问

手机连接可访问宿主机的公司内网 Wi-Fi 后，在手机浏览器打开 http://192.168.110.54:8080/ ，使用飞书登录。访客 Wi-Fi 若隔离内网设备将无法访问；当前部署未提供蜂窝网络或外网入口。

手机宽度默认显示卡片列表；上传及详情窗口适配全屏，表单为单列，控件支持触摸及安全区。文件选择通过系统文件选择器完成，压缩包可从手机的“文件”应用选择。具体文件选择和下载体验依赖手机浏览器，需要真机验证。

2026-09-03 手机适配已构建部署；尚未完成 iOS / Android 真机验收。若需要从飞书工作台打开，应将飞书网页应用的移动端主页配置为上述局域网首页，按控制台要求发布生效；应用可用范围和平台角色配置仍需满足。

## 文件预览

Demo 详情 → 文件清单 → 预览：支持浏览器可解码的视频/音频、常见图片、文本与代码（按纯文本显示，不执行 HTML 或脚本）。独立媒体支持 Range 请求和进度拖动。

ZIP 可逐层打开文件夹，点击包内文件预览。RAR、7z、嵌套 ZIP 暂不展开，需下载查看。文本最多显示前 1 MB；ZIP 最多 10000 条目，包内单文件最多 64 MB，限制异常解压比例，最多同时处理 4 个压缩包请求。加密条目提示下载解密。

预览和媒体请求每次验证当前用户可见范围与版本状态；下架版本不可预览。ZIP 只按条目读取，不解压落盘。已验证跨用户访问拒绝、ZIP 内代码、脚本按文本展示、媒体分段读取和下架后拒绝访问。

## 飞书审核通知

员工点击提交审核后，将提醒其当前负责主管；主管从待审核状态审核通过并发布后，抄送平台内所有已启用的产品经理。主管直接发布自己的草稿不触发本轮“审核通过”抄送。草稿上传不发送消息。

队列与业务变更在同一个数据库事务中提交，服务每 5 秒处理一次；失败最多尝试 5 次，保留飞书请求去重 UUID。发送前复核角色、主管归属、版本状态及审核轮次。通知内容包含 Demo 名称、版本、员工及平台详情入口，不发送原文件。发送失败不回滚业务操作。

管理员可在“平台设置 → 飞书通知”查看最近 100 条记录，并在修复配置后手动重试失败记录。没有产品经理时记录未配置接收人，不向管理员或其他员工替代抄送。历史已提交/已发布数据不补发。

2026-09-03 接口检查：应用鉴权成功，但 bot/v3/info 返回 11205（app do not have bot）。需要在飞书开发者后台启用机器人能力，申请应用身份权限 im:message:send_as_bot，并发布审核生效；可用范围须包含主管与产品经理。真实消息送达尚待该配置完成后联调。业务及模拟飞书发送测试已通过。

2026-09-03 18:15：已在飞书后台添加机器人能力，开通 im:message:send_as_bot，并发布版本 1.0.4。页面确认“当前修改均已发布”；线上 bot/v3/info 返回 code=0，机器人已生效。发布后通知队列为空，尚无真实审核事件验证消息送达。后续员工提交审核时按上述规则发送，发送结果可在平台设置查看。

2026-09-04：飞书通知已扩展为完整工作流。员工提交审核、撤回通知当前负责主管；主管退回返修通知归属员工并附处理意见；发布、下架、恢复发布、设为默认版本通知归属员工及全部已启用产品经理。操作人不会收到自己的重复通知，兼具多个角色的同一接收人只发送一次。发送前仍复核状态与当前角色关系，过时任务自动取消。完整状态流和接收人测试已通过，宿主机已重启部署。

2026-09-04：飞书通知入口已改为网页应用 AppLink（client/web_app/open），携带 demo 参数；点击消息由飞书客户端打开应用并定位 Demo 详情，不再直接展示宿主机网页地址。

## 页面式交互

2026-09-04：Demo 上传、详情、编辑、成员配置和资产交接由居中遮罩弹窗改为工作区内独立页面。页面保留侧栏与顶部导航，使用明确的“返回”操作；Demo 详情和新建页同步 URL 状态，支持浏览器返回。审核发布等危险动作仍使用局部确认层。桌面端内容区最大宽度约 1120px，移动端使用全屏页面。
